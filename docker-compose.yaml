version: '3.8'

services:
#  postgres:
#    container_name: proxy-db
#    image: postgres:17
#    ports:
#      - "5434:5432"
#    environment:
#      POSTGRES_DB: proxydb
#      POSTGRES_USER: username
#      POSTGRES_PASSWORD: password
#    volumes:
#      - proxy_pg_data:/var/lib/postgresql/data
#      - ./init_db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
#    networks:
#      - proxy_network
#    healthcheck:
#      test: [ "CMD-SHELL", "pg_isready -U username -d proxydb" ]
#      interval: 10s
#      timeout: 5s
#      retries: 5

  proxy:
    container_name: proxy-service
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL}
      FOLDER_PATH: /app/proxy_list
      CHECK_DB_INTERVAL_MINUTES: ${CHECK_DB_INTERVAL_MINUTES}
      MIN_PROXIES_LIMIT: ${MIN_PROXIES_LIMIT}
      CHECK_FOLDER_INTERVAL_MINUTES: ${CHECK_FOLDER_INTERVAL_MINUTES}
      PROXIES_FAILED_ATTEMPTS: ${PROXIES_FAILED_ATTEMPTS}
      TIME_OF_CHECKING_PROXY_SECONDS: ${TIME_OF_CHECKING_PROXY_SECONDS}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      TO_EMAIL: ${TO_EMAIL}
    volumes:
      # Связываем физическую папку proxy_list на сервере с папкой внутри контейнера
      - ./proxy_list:/app/proxy_list
#    depends_on:
#      postgres:
#        condition: service_healthy
    networks:
      - proxy_network

#volumes:
#  proxy_pg_data:
networks:
  proxy_network:
    name: proxy_adapter_proxy_network
    external: true